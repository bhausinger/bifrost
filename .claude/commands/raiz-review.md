Review the recent changes (use `git diff HEAD~1` or `git diff --staged`).

Check for:
- Logic errors or bugs
- Missing error handling
- Code duplication
- Security issues (exposed secrets, injection, unsafe input)

Format: list issues by severity (Critical → Warning → Suggestion) with file paths and line numbers.